from setuptools import setup, find_packages

setup(name='lo-analysis',
      version='0.0.01',
      description='',
      author='',
      author_email='',
      packages=find_packages(exclude=('tests')),
      zip_safe=False)
